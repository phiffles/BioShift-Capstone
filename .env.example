# Copy this file to .env and add local values there.
# Never place real credentials in this tracked example file.

# Add service-specific variables only when an integration requires them:
# SERVICE_API_KEY=replace-with-your-local-api-key
